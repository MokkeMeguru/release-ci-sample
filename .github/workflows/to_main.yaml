name: release to main

on:
  pull_request:
    types: [opened, synchronize]
    branches:
      - main

jobs:
  checkout:
    name: hello_for_main
    runs-on:
      labels: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: echo hello
        run: |
          echo "hello world for release to main"
